#include <stdio.h>
/* A program that prints the given integers on the screen in hex format */
int main()
{
	int a, b, c;
	a = 15;
	b = 150;
	c = 1500;

	printf("The numbers 15, 150 and 1500 in hexadecimal are: %X, %X and %X respectively", a, b, c);

	return (0);
}
